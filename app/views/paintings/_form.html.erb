<%= form_for(@painting, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @painting.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@painting.errors.count, "error") %> prohibited this painting from being saved:</h4>

      <ul>
      <% @painting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">

      <%= f.label :title, class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :title, class: "form-control" %>
      </div>

  </div>
  <div class="form-group">

      <%= f.label :painting_kind, class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.hidden_field :painting_kind_id %>
        <%= autocomplete_field_tag 'painting_kind_title', '', autocomplete_painting_kind_title_paintings_path, id_element: '#painting_painting_kind_id' %>
      </div>

  </div>
  <div class="form-group">

      <%= f.label :user, class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.hidden_field :user_id %>
        <%= autocomplete_field_tag 'user_email', '', autocomplete_user_email_paintings_path, id_element: '#painting_user_id' %>
      </div>

  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
